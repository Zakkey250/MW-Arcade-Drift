# MW Arcade Drift 0.1.1 — 設定ガイド

編集アプリは任意の別ダウンロードです。コアだけならEXEは不要で、テキストエディターによるJSON編集・保存後にCtrl+DをOFF→ONで反映できます。以下のGUI操作説明は編集アプリを追加した場合のものです。

## 起動と画面
`scripts/MWArcadeDrift/Configurator.exe` を起動します。追加ランタイムのダウンロードは不要です（Windows の .NET Framework 4.x を使用）。

左上で「デフォルト」または作成済みの車種を選択し、「ドリフト」「カメラ」のタブを切り替えます。現在の編集先とファイルパスを常時表示します。簡易モードは各画面3項目、アドバンスはドリフト17項目・カメラ20項目とそれぞれの有効スイッチです。

簡易モードの50は配布時の標準値です。保存・再読込・モード切替後も、保存済みの数値からつまみの位置を復元します。アドバンスで同じグループの項目を個別に変えた場合、1本の簡易スライダーでは現在値を近似表示します。そのつまみを動かすと、グループ内の項目を配布時の標準値からまとめて再計算します。モード切替前に確認を表示します。最後に使った編集モードはデフォルトと各車種で別々に保存し、各プロファイルと同じフォルダーの編集アプリ専用 `editor_ui.json` を使用します。ゲームはこのファイルを読みません。

* ドリフト: 旋回力、滑り角とハンドブレーキ、速度維持。
* カメラ: 近さ、回り込みと前方の見通し、動きの穏やかさ。
* 言語: システム自動 / 日本語 / English。手動指定はアプリとゲーム通知に共通です。

## ファイル構成
```
scripts/MWArcadeDrift/
  default_camera.json
  default_drift.json
  settings.json
  Configurator.exe
  ConfigCheck.exe
  fields.json
  editor_ui.json  （デフォルトの編集モード。切替時に作成）
  vehicles/
    SVJ/
      camera.json
      drift.json
      editor_ui.json  （SVJの編集モード。切替時に作成）
```

初めて乗車した車両について、ゲーム内部の車種名でフォルダーを作り、デフォルトの2ファイルを複製します。片方だけ欠けていれば、そのファイルだけ補います。作成済みの設定は上書きしません。デフォルトを変更しても既存の車種設定へは一括反映されません。車種一覧を更新するにはアプリの「再読込」を使います。

ゲーム内の Ctrl+D でドリフトモードを切り替えます。ONにする時に、その車のファイルと共通設定を読み直します。編集中の内容は「保存」してから OFF → ON にしてください。走行中の継続ポーリングは行いません。一時停止の復帰だけでは再読込しません。

車種の drift.json の enabled=false は、その車のドリフト禁止です。この場合Ctrl+Dを押しても有効にはなりません。アプリで有効にして保存し、再度ON操作をしてください。camera.json の enabled=false はカメラ演出のみを止めます。Ctrl+Dによる一時的なOFFはJSONに書き戻しません。

## HUDと通知
スリップ警告灯風の小アイコンは、緑=モードON、アンバー=ドリフト中、グレー=OFFです。メニュー・無効なプレイヤー状態では非表示です。

Ctrl+Dの結果はゲームの汎用メッセージに表示します。日本語フォントで必要な文字を表現できない環境では英語へフォールバックします。日英以外のOS言語では英語を使います。汎用メッセージの関数が検証できない環境では、通知のみ停止します。

settings.json の hud で位置、サイズ、不透明度と表示可否を調整できます。これらは共通HUD設定のため、ドリフト・カメラ編集画面には混在させていません。

## 編集の保護
未保存の変更がある状態で終了、車種切替、再読込、フォルダー変更をすると「保存・破棄・キャンセル」を表示します。EnterとEscはキャンセルとして扱います。保存に失敗した場合は終了や切替を止め、編集内容を残します。

保存前にゲームと同じC++パーサーで数値範囲と組合せを検証します。外部でファイルが変更されていれば上書きを拒否します。保存の直前に元の2ファイルと保存先の情報を backups/ 配下へ退避します。ファイル置換の失敗時は退避した内容へ戻します。ゲームとアプリは同じロックファイルを用いて同時読み書きを避けます。タスクの強制終了・電源断に対する未保存バッファの復元機能はありません。

JSONエラー時、ゲームはその車両のドリフト補助を無効にしてログへ記録します。直してからON操作で再試行できます。既存のv1 drift.json/camera.json/vehicles.json は移行時に残しますが、新版は上記のv2ファイルを参照します。

## 検証範囲
コントローラー回帰、プロファイル複製と検証、編集終了保護、バックアップ・競合検出、独立D3D9描画と日英UIをオフライン検証しています。実ゲームでの車種名、自動作成、Ctrl+D、汎用メッセージ表示、既存メーターとの位置関係は実走で確認してください。ハンドリングとカメラのデフォルト数値は承認済みの基準を維持しています。

N2O連携APIは未追加です。利用側の仕様が決まった時点で、必要な状態参照を最小限に設計します。

## 駆動方式別の演出（v0.1.0）
車両データの前輪への駆動配分から自動判定します。FWDではカウンター表示を残し、前後輪の空転と回転数の上乗せを行いません。既存の横滑り挙動と通常の車輪回転で表現します。RWDは後輪、AWD／4WDは全輪に空転演出を加えます。判定不能時も空転・回転数の上乗せを止めます。すべての駆動方式で舵角の滑らかな復帰を維持します。新たな荷重移動の物理計算は追加せず、タイヤ力・駆動配分・ハンドリング値・AT変速判断は変更しません。

アイコンは乗車後にD3D9のPresentからバックバッファへ描画します。ゲーム本体の描画呼び出し命令は書き換えません。緑＝有効、アンバー＝ドリフト中、灰＝無効です。単独D3D検証は通過しています。先行alpha.4の実走でも起動とアイコン描画に目立つ不具合は報告されていません。FWD車は今回の走行記録に含まれていません。

調整順序：まずゲーム内の「パフォーマンスチューン」でハンドリングを調整します。そこで足りない場合に、任意アプリでデフォルトまたは車種別プロファイルを調整してください。保存後は Ctrl+D のOFF→ONで再読込します。
